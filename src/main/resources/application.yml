server:
  port: 8080

spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: https://login.microsoftonline.com/bfbc730b-74fb-4552-b74c-c93431273927/v2.0
          
  application:
    name: institution-service


  datasource:
    url: jdbc:postgresql://localhost:5432/institucion_db
    username: postgres
    password: "simurdiera77"
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

logging:
  level:
    com.athenet.institution: DEBUG